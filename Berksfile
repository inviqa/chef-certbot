source 'https://supermarket.chef.io'

cookbook 'config-driven-helper', :github => 'inviqa/chef-config-driven-helper', :branch => 'feature/generate-self-signed-ssl'

group :integration do
  cookbook 'fake', :path => 'spec/cookbooks/fake'
end

metadata
